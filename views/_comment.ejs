
<li class="mt-3" id="comment-<%= comment._id %>">
        <nav class="navbar navbar-expand post-comment" style="box-shadow: 5px 5px 3px 0px gray;background-color: rgb(248, 240, 240)">
            <div class="container-fluid">
              <a class="navbar-brand" href="#">
    
                <!-- for the like -->
                <img src="<%= comment.user.avtar %>" style="border-radius: 50%; width: 30px; height: 30px;"
                  class="img-fluid mt-2" alt="...">
              </a>
              <!-- for the comment -->
              <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                  <li class="nav-item">
                  
                      <%= comment.content %> 
                    
                  </li>
                </ul>
                <!-- for the share -->
                <form class="d-flex">
                    <%if((locals.user && locals.user.id==comment.user.id)||locals.user && locals.user.id==post.user.id){%>
                        <small>
                            <a href="/comment/delete/<%=comment.id%>"> <img width="30px" src="https://cdn-icons-png.flaticon.com/128/1617/1617543.png" alt=""></a>
                           
                        </small>
                        <%}%>
                </form>
              </div>
    
            </div>
          </nav>
        <%= post.user.name %>
        

        
        
</li>